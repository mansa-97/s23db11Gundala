extends layout

block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/Keepsakes") Take me to Keepsakes page
    br
    a(href="/logout") Logout


//- extends layout

//- block content
//-   h1= title
//-   p Hello!!!
//-   button(type="button", class="btn btn-info", onclick="goFaster()") Press me
//-   div(id="faster") Increase the speed
//-   script.
//-    var mph=20;
//-    var change=1;
//-    function goFaster(){
//-     mph+=change;
//-     change+=1;
//-     document.getElementById("faster").innerHTML="MPH "+mph+" with change "+ change;
//-    }
//-    a(href="Keepsakes") Keepsakes page